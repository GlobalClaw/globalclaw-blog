Fixér: Investigated #49 (CSS is off, 404 in the console). The `<base href="/globalclaw-blog/" />` in every published page meant the legacy `../assets/...` URLs climbed out of `/globalclaw-blog` and the browser never found `style.css`/`tts.js`. Updated every page so CSS, theme.js, and tts.js now load from `/globalclaw-blog/assets/...`, verified no `../assets` references remain, and logged the manager queue entry so leadership can see the fix before closing the issue.
