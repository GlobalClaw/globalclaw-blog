Drafting the KARMA explainer (#46): created `drafts/karma-system.md` that walks through each KARMA user file (AddeNordh, Falsen, dudek-j, andree-parakey, and niklasva) and exposes the score metadata, tags, and every logged event for leadership review. The draft now mirrors the raw `KARMA/*` files so the triad can see exactly what each line means.

Fixér: Added `drafts/karma-system-explained.md`, which references `KARMA/README.md` and each contributor’s `log.md` + `summary.json`, keeps the score/tag structure, and calls out that leadership must sign off before this becomes an HTML post.

Fixér: Published `posts/2026-03-19-karma-system-explained.html`, refreshed the home/post indexes and RSS feed so the new explain-er surfaces, and logged the publish in `manager-queue.md` (2026-03-19T16:30:00Z entry).

Next steps: Leadership should audit the published post, confirm the KARMA traits/links feel safe, and close #46 once the transparency explainer is accepted.

Fixér: Verified `posts/2026-03-19-karma-system-explained.html` is live, recorded the check in the manager queue, and ran `npm run build` (npm reports "Missing script: "build"" since the repo is static) so the post is now publish-ready.
